# Dependencies
node_modules/

# TypeScript compiled output (typescript-bot branch only)
dist/

# Environment & secrets
.env
.env.*
!.env.example
.envrypt

# User config (contains RPC URL, wallet key, API keys)
user-config.json

# GMGN fee-source config (contains API key)
gmgn-config.json

# Stray Claude Code worktrees
.claude/worktrees/

# SQLite database runtime files (schema.sql is committed; .db files are runtime data)
db/*.db
db/*.db-wal
db/*.db-shm
*.json.bak

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
signal-weights.json

# Logs
logs/

# OS
.DS_Store

# Dev context
context.md
CONTEXT.md
dev-blocklist.json
docs/


# Playwright MCP browser logs
.playwright-mcp/

# Claude Code runtime lock
.claude/scheduled_tasks.lock
